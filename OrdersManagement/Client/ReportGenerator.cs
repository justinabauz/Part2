using System;
namespace DataBL
{
    public class ReportGenerator
    {
        /*private ClientRepository _clientRepository;
        public ReportGenerator(ClientRepository clientRepository)
        {
            _clientRepository = clientRepository;
        }
        public string GenerateCompanyReport()
        {
            //List<Client> client = _clientRepository.Retrieve();
            //string clientTemp = "";
            //foreach (var oneClient in client)
           //{
                //clientTemp += "ID: " + oneClient.clientID + " Name: " + oneClient.Name + ;
            //}
            //return clientTemp;
        }

*/
    }
}